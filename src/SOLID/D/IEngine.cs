// abstraction 

// that's why definition is : higher level module not depend on lower level module 
                              // but both depend on abstraction ( interface ).

namespace DesignPatternLearning.src.SOLID.D
{
    public interface IEngine
    {
        void start();
    }
}
